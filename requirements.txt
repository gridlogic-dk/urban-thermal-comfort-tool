# Core pipeline (stages 1-5)
pandas>=2.2.0
numpy>=1.26.0
pyyaml>=6.0
pillow>=10.0.0
tqdm>=4.66.0

# Stage 0 - street-view semantic segmentation
# See https://zensvi.readthedocs.io/ for install notes and GPU requirements.
zensvi>=0.13.0

# Optional - only needed if you extend stage3 with spatial joins/CRS work.
# geopandas>=0.14.0
# shapely>=2.0.0
# pyproj>=3.6.0
